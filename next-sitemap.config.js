/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://rematch-cookbook.vercel.app',
    generateRobotsTxt: true,
    outDir: './public', // This tells it to generate in the public folder
  };
  